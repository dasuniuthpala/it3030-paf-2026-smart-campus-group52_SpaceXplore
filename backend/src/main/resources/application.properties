spring.application.name=demo

spring.datasource.url=jdbc:postgresql://ep-shiny-firefly-anrvh34f-pooler.c-6.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&sslNegotiation=direct&connectTimeout=30&socketTimeout=30
spring.datasource.username=neondb_owner
spring.datasource.password=npg_2Cc0SYAkaubP

spring.datasource.driver-class-name=org.postgresql.Driver




# #Create PostgreSQL DB as "SpaceXplore" in pgAdmin
# spring.datasource.url=jdbc:postgresql://localhost:5432/SpaceXplore
# # add your PostgreSQL username and password
# spring.datasource.username=postgres
# spring.datasource.password=postgres

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

#Change port if you need
server.port=8086

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000

# Google OAuth2 Configuration Placeholders
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile, email